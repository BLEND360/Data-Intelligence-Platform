# CLARITY Frontend Environment Variables
# Copy this file to .env.local for local development

# API Base URL
# For local development, point to the backend directly
VITE_API_URL=http://127.0.0.1:8082

# For production (SPCS deployment), this is set to /api in the Dockerfile
# The nginx proxy will route /api/* requests to the backend service
